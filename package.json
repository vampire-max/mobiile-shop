{
  "name": "Swiper - Slides per view auto",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tailwind build src/css/style.css -c ./tailwind.config.js -o dist/css/style.css",
    "build:css": "tailwind build src/css/style.css -c ./tailwind.config.js -o dist/css/style.css",
    "prod:css": "tailwindcss -i src/style.css -c ./tailwind.config.js -o dist/css/style.css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.3.7",
    "postcss": "^8.3.9",
    "tailwindcss": "^2.2.17"
  },
  "tags": [
    "swiper"
  ],
  "dependencies": {
    "swiper": "^7.2.0"
  }
}
